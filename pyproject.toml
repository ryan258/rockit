[project]
name = "rockit"
version = "0.1.0"
description = "Beat Saber to Ragnarock VR converter pipeline with audio warping"
readme = "README.md"
requires-python = "==3.11.*"
dependencies = [
    "demucs>=4.0.1",
    "librosa>=0.11.0",
    "pyrubberband>=0.4.0",
    "soundfile>=0.13.1",
]
